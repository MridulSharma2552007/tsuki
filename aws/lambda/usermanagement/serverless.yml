service: tsuki-usermanagement

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: ap-south-1
  stage: prod

functions:
  health:
    handler: handler.handler
    events:
      - httpApi:
          path: /health
          method: get
